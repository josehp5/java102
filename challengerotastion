public class Point {
    private int x, y;
    public Point(int x, int y) {
        this.x = x;
        this.y = y;
    }

    public Point rotate() {
        return new Point(-this.y, this.x);
    }

    public void display() {
        System.out.println("Point(" + x + ", " + y + ")");
    }

